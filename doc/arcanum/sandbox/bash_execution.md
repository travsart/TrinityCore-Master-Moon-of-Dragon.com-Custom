---
description: "bash execution internals — shell init profile loading, working directory persistence, timeout handling, background commands, Windows MSYS2 Git Bash"
---

# Bash Execution Internals
> Source: `utils/bash/`, `utils/shell/`
> Status: STUB — needs research

## Key Questions
- How does the Bash tool actually execute commands?
- Shell initialization (profile loading, env vars)
- Working directory persistence between calls
- Timeout handling and process management
- Background command tracking (run_in_background)
- Platform differences (bash on Windows via MSYS2/Git Bash)

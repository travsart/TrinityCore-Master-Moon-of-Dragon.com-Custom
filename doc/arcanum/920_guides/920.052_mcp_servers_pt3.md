---
title: "Guide: MCP Servers — Building & Connecting — Arcanum Wiki (Part 3/6)"
description: "MCP server guide — building connecting, stdio transport, tool registration, server instructions, OAuth PKCE, 27.8h timeout, NotebookLM MCP vision"
tags: [guides, building-connecting, stdio-transport, tool-registration, server-instructions, oauth-pkce, 278h-timeout, notebooklm-mcp]
part: 3
parts: 6
---

## Building an MCP Server (Python Example)

Here's a minimal MCP server in Python:

```python
#!/usr/bin/env python3
"""Minimal MCP server example."""
import json
import sys

def read_message():
    """Read a JSON-RPC message from stdin."""
    line = sys.stdin.readline()
    if not line:
        return None
    return json.loads(line)

def write_message(msg):
    """Write a JSON-RPC message to stdout."""
    sys.stdout.write(json.dumps(msg) + "\n")
    sys.stdout.flush()

def handle_initialize(msg):
    return {
        "jsonrpc": "2.0",
        "id": msg["id"],
        "result": {
            "protocolVersion": "2024-11-05",
            "capabilities": {
                "tools": {}
            },
            "serverInfo": {
                "name": "my-server",
                "version": "1.0.0"
            }
        }
    }

def handle_tools_list(msg):
    return {
        "jsonrpc": "2.0",
        "id": msg["id"],
        "result": {
            "tools": [
                {
                    "name": "hello",
                    "description": "Say hello to someone",
                    "inputSchema": {
                        "type": "object",
                        "properties": {
                            "name": {
                                "type": "string",
                                "description": "Name to greet"
                            }
                        },
                        "required": ["name"]
                    }
                }
            ]
        }
    }

def handle_tool_call(msg):
    params = msg.get("params", {})
    tool_name = params.get("name")
    arguments = params.get("arguments", {})

---
[Part 1](920.050_mcp_servers_pt1.md) | [Part 2](920.051_mcp_servers_pt2.md) | **Part 3** | [Part 4](920.053_mcp_servers_pt4.md) | [Part 5](920.054_mcp_servers_pt5.md) | [Part 6](920.055_mcp_servers_pt6.md)

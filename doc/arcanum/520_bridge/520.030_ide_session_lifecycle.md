---
description: "IDE session lifecycle — session spawn, permission relay, state sync, reconnection, VS Code extension lifecycle"
title: "IDE Session Lifecycle"
tags: [bridge, session-spawn, permission-relay, state-sync, reconnection]
---

# IDE Session Lifecycle
> Source: `17_bridge.md`

## Session Creation

1. CLI registers environment or creates session directly (v2)
2. Remote user connects via claude.ai/code or mobile app
3. CLI receives work item, spawns child process
4. Session ID and transport credentials exchanged
5. Bidirectional event streaming begins

## Crash Recovery

`bridgePointer.json` persisted per-directory with session/environment IDs, 4-hour TTL. `readBridgePointerAcrossWorktrees()` fans out for `--continue` support. Cleared on clean shutdown.

## Token Refresh (Two Layers)

1. **Worker JWT** (from `/bridge`): refreshed proactively 5min before expiry
2. **OAuth token** (from keychain): refreshed via `withOAuthRetry()` on 401

## Session Timeout

Default 24 hours (`DEFAULT_SESSION_TIMEOUT_MS`), configurable via `--session-timeout`. Watchdog timer kills exceeded sessions.

## Multi-Session Spawn Modes

| Mode | Behavior |
|------|----------|
| `single-session` | One session, bridge exits when done |
| `worktree` | Isolated git worktree per session |
| `same-dir` | Sessions share cwd (can conflict) |

## Resilience Patterns

- **Exponential backoff**: 2s initial, 120s cap, 600s give-up
- **Sleep/wake detection**: Poll > 2x cap = laptop slept, reset budget
- **Heartbeat leases**: Extend work item lease via POST
- **Graceful shutdown**: SIGTERM + 30s -> SIGKILL cascade
- **Transport swap**: v2 rebuilds on epoch mismatch (409)

## Key Source Files

| File | Purpose |
|------|---------|
| `src/bridge/bridgeMain.ts` | Session spawn and lifecycle |
| `src/bridge/initReplBridge.ts` | REPL bridge initialization |

## Cross-References

- [Bridge Architecture](520.020_bridge_architecture.md) -- overall design
- [WebSocket Protocol](520.050_websocket_protocol.md) -- message format

---
description: "plugin system — installation management, marketplace integration, bundled plugins, plugin-provided tools commands agents hooks, createMovedToPluginCommand"
---

# Plugin System
> Source: `src/plugins/`, `utils/plugins/`, `services/plugins/`
> Status: STUB — needs research

## Key Questions
- What is the plugin system? How does it differ from skills?
- Plugin loading, activation, lifecycle
- Bundled plugins (`src/plugins/bundled/`) — what ships?
- The `/plugin` and `/reload-plugins` commands
- DXT extensions (`utils/dxt/`) — what are these?

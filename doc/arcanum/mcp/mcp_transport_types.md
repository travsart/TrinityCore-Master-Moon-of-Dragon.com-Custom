---
description: "MCP transport types — stdio, SSE, streamable, websocket, HTTP, docker, npx, uvx, 8 transport backends, env var expansion"
---

# MCP Transport Types
> Source: `utils/mcp/`
> Status: STUB — needs research

## Key Questions
- stdio vs SSE vs WebSocket vs HTTP — capabilities of each
- Which transport supports streaming? Bidirectional?
- How does Claude Code choose transport for a given server config?

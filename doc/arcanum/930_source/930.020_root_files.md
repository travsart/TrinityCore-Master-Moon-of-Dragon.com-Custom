---
description: "root source files — Tool.ts buildTool factory, query.ts 1729-line main loop, QueryEngine.ts, commands.ts registry, context.ts assembly, setup.ts bootstrap"
---

# Root Source Files
> Source: `06_tool_pipeline.md`, `20_messages_pipeline.md`

## The Big 10

| File | Size | Role |
|------|------|------|
| `query.ts` | ~70KB | Main conversation loop -- the brain of CC |
| `QueryEngine.ts` | ~48KB | SDK/headless engine wrapper |
| `Tool.ts` | ~30KB | `Tool<I,O,P>` interface, `buildTool()` factory, `ToolUseContext` |
| `commands.ts` | ~25KB | 86+ slash commands, skill loading, command registry |
| `tools.ts` | ~15KB | Tool registry: `getAllBaseTools()`, `assembleToolPool()` |
| `setup.ts` | ~15KB | Session initialization, settings load |
| `context.ts` | ~7KB | `getUserContext()` (CLAUDE.md), `getSystemContext()` (git) |
| `history.ts` | ~15KB | JSONL prompt history, session resume |
| `Task.ts` | ~5KB | Task type definitions |
| `cost-tracker.ts` | ~11KB | Token/cost tracking, session persistence |

## Hot Path Files (Called Every Turn)

- `query.ts` -- orchestrates each iteration
- `services/api/claude.ts` (129KB) -- builds and sends API request
- `utils/messages.ts` -- normalizes messages for API
- `utils/attachments.ts` -- evaluates ~30 attachment providers
- `constants/prompts.ts` -- regenerates system prompt sections

## Key Factories

- `buildTool()` (Tool.ts) -- creates tools with fail-closed defaults (`isConcurrencySafe: false`, `isReadOnly: false`)
- `assembleToolPool()` (tools.ts) -- merges built-in + MCP, sorted for cache stability
- `getCommands()` (commands.ts) -- returns all commands including dynamic skills

## Key Source Files

| File | Purpose |
|------|---------|
| `src/query.ts` | Conversation loop |
| `src/Tool.ts` | Tool interface and factory |
| `src/tools.ts` | Tool registry and assembly |

## Cross-References

- [Constants and Prompts](930.010_constants_and_prompts.md) -- hardcoded values
- [Query Loop](../220_query/220.010_query_loop.md) -- detailed query.ts analysis
